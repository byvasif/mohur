{
  "name": "mohur",
  "version": "0.1.0",
  "private": true,
  "description": "Avadanlıq müayinə sertifikatlarının Google Sheet sətrindən üretilməsi",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "build": "node build/bundle.mjs",
    "push": "npm run build && clasp push"
  },
  "devDependencies": {
    "@types/google-apps-script": "^2.0.13",
    "esbuild": "^0.28.2",
    "typescript": "^7.0.2",
    "vitest": "^5.0.0"
  }
}
